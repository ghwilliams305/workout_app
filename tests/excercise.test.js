const findExcercise = require("../modules/excercise");

describe('findExcercise function', () => {
    const excerciseOne = {
        
    }

    test('returns an object', () => {
        const result = findExcercise();

        expect()
    });
});